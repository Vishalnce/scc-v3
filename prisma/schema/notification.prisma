model Notification {
  id        String   @id @default(cuid())
  title     String
  path      String
  type      NotificationType
  entityId  Int
  createdAt DateTime @default(now())
}

enum NotificationType {
  QUIZ
  BLOG
  EXAM
  CONCEPT
  CURRENT_AFFAIR
  EDITORIAL
}